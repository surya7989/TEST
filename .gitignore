# Node & Dependencies
node_modules/
package-lock.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Production Build Outputs (intermediary dirs only — root-level build files ARE tracked for Hostinger Git deploy)
build/
hostinger_public_html/
deploy.zip
*.zip

# dist/ is a duplicate of root build output — not needed in repo
dist/

# Environment & Secrets (NEVER COMMIT)
.env
.env.*
*.env
!.env.example
apps/*/.env
apps/*/.env.*
apps/*/*.env
!apps/*/.env.example

# Logs & Temporary Files
logs/
*.log
*.tmp
*.bak
.system_generated/
scratch/

# OS Metadata
.DS_Store
Thumbs.db
ehthumbs.db

# IDE & Editor Files
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
